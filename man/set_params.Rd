% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_params.R
\name{set_params}
\alias{set_params}
\title{Set simulation parameters}
\usage{
set_params(
  type = "summary",
  sample_size_Xs = 1e+05,
  sample_size_Y = 1e+05,
  number_of_exposures = 1,
  number_of_causal_SNPs = 100,
  prop_gwas_overlap_Xs_and_Y = 1,
  prop_gwas_overlap_Xs = 1,
  number_of_UHP_causal_SNPs = 0,
  number_of_CHP_causal_SNPs = 0,
  ratio_of_UHP_variance = 0,
  ratio_of_CHP_variance = 0,
  CHP_correlation = 0,
  Y_variance_explained_by_UHP = 0,
  U_variance_explained_by_CHP = 0,
  true_causal_effects = 0.3,
  Y_variance_explained_by_Xs = 0.3,
  signs_of_causal_effects = 1,
  phenotypic_correlation_Xs = 0.3,
  genetic_correlation_Xs = 0.15,
  phenotypic_correlations_Xs_and_Y = 0.3,
  Xs_variance_explained_by_g = 0.1,
  LD_causal_SNPs = "I",
  number_of_LD_blocks = 1,
  Xs_variance_explained_by_U = 0,
  Y_variance_explained_by_U = 0,
  simtype = "winners",
  IV_Pvalue_threshold = 5e-08,
  fix_Fstatistic_at = 10,
  MVMR_IV_selection_type = "union",
  LD_pruning_r2 = 1,
  MR_standardization = "none",
  N_of_LD_ref = Inf
)
}
\arguments{
\item{type}{\code{"summary"} (default) or \code{"individual"}.

--- Study design ---}

\item{sample_size_Xs}{Exposure GWAS sample size(s). Scalar or vector with one value per exposure.}

\item{sample_size_Y}{Outcome GWAS sample size.}

\item{number_of_exposures}{Number of exposures.}

\item{number_of_causal_SNPs}{Number of SNPs with a direct effect on each exposure.

--- GWAS overlap ---}

\item{prop_gwas_overlap_Xs_and_Y}{Proportion of overlap between exposure and outcome GWAS. Scalar or vector.}

\item{prop_gwas_overlap_Xs}{Proportion of overlap among the exposure GWAS (summary only). Scalar or numeric matrix.

--- Pleiotropy (summary data) ---}

\item{number_of_UHP_causal_SNPs}{Number of uncorrelated horizontal pleiotropy (UHP) SNPs.}

\item{number_of_CHP_causal_SNPs}{Number of correlated horizontal pleiotropy (CHP) SNPs.}

\item{ratio_of_UHP_variance}{Ratio of UHP variance to valid-IV variance.}

\item{ratio_of_CHP_variance}{Ratio of CHP variance to valid-IV variance.}

\item{CHP_correlation}{Correlation between CHP and valid-IV effect sizes (magnitude of CHP).

--- Pleiotropy (individual data) ---}

\item{Y_variance_explained_by_UHP}{Outcome variance explained by UHP SNPs.}

\item{U_variance_explained_by_CHP}{Confounder variance explained by CHP SNPs.

--- Causal effects ---}

\item{true_causal_effects}{True causal effect size(s). Scalar or vector (summary only).}

\item{Y_variance_explained_by_Xs}{Outcome variance explained by each exposure. Scalar or vector (individual only).}

\item{signs_of_causal_effects}{Signs of causal effects. Scalar or vector (individual only).

--- Correlations ---}

\item{phenotypic_correlation_Xs}{Phenotypic correlations among exposures. Scalar, string (\code{'ar1(0.5)'}, \code{'cs(0.3)'}, \code{'toeplitz'}), or matrix.}

\item{genetic_correlation_Xs}{Genetic correlations among exposures. Same formats as above.}

\item{phenotypic_correlations_Xs_and_Y}{Phenotypic correlations between each exposure and the outcome. Scalar or vector (summary only).

--- Genetic architecture ---}

\item{Xs_variance_explained_by_g}{Heritability of each exposure (variance explained by all causal SNPs). Scalar or vector.}

\item{LD_causal_SNPs}{LD structure among causal SNPs. Scalar, string (\code{'I'}, \code{'toeplitz'}, \code{'ar1(0.5)'}), or numeric matrix.}

\item{number_of_LD_blocks}{Number of independent LD blocks.

--- Confounding (individual only) ---}

\item{Xs_variance_explained_by_U}{Exposure variance explained by the latent confounder.}

\item{Y_variance_explained_by_U}{Outcome variance explained by the latent confounder.

--- IV selection ---}

\item{simtype}{\code{"winners"} (P-value-based selection) or \code{"weak"} (fix F-statistic).}

\item{IV_Pvalue_threshold}{P-value threshold for IV selection (used when \code{simtype = "winners"}).}

\item{fix_Fstatistic_at}{Target mean F-statistic (used when \code{simtype = "weak"}).}

\item{MVMR_IV_selection_type}{\code{"union"} or \code{"joint"} (multivariable MR only).}

\item{LD_pruning_r2}{Upper r² threshold for LD pruning of IVs.

--- Output ---}

\item{MR_standardization}{Standardization applied to GWAS summary statistics. \code{"none"}, \code{"Z"}, or \code{"QC"}.}

\item{N_of_LD_ref}{Sample size of the LD reference panel (\code{Inf} = use true LD).}
}
\value{
A named list of parameters ready to pass to \code{generate_summary()} or \code{generate_individual()}.
}
\description{
Constructs a named parameter list for use with \code{generate_summary()} or
\code{generate_individual()}.  Every argument has a sensible default so you
only need to specify the values you want to change.
}
\examples{
# Minimal: one exposure, default settings
params <- set_params()
data   <- generate_summary(params)

# Two exposures with CHP, no GWAS overlap
params <- set_params(
  number_of_exposures        = 2,
  true_causal_effects        = c(0.3, 0.1),
  prop_gwas_overlap_Xs_and_Y = 0,
  number_of_CHP_causal_SNPs  = 20,
  ratio_of_CHP_variance      = 0.25,
  CHP_correlation            = -0.5
)
data <- generate_summary(params)
}
