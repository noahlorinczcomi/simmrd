% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_params.R
\name{load_preset}
\alias{load_preset}
\title{Load a named simulation preset}
\usage{
load_preset(
  bias = "none",
  n = 1e+05,
  snps = 100,
  exposures = 1,
  overlap = "full"
)
}
\arguments{
\item{bias}{Bias scenario. One of \code{"none"}, \code{"UHP"},
\code{"CHP"}, \code{"UHP_CHP"}, \code{"UHP_CHP_WEAK"}, or \code{"WEAK"}.}

\item{n}{GWAS sample size for both exposures and outcome. \code{3e4} or \code{1e5}.}

\item{snps}{Number of causal SNPs per exposure. \code{100} or \code{500}.}

\item{exposures}{Number of exposures. \code{1} or \code{3}.}

\item{overlap}{\code{"full"} (complete exposure–outcome overlap) or
\code{"none"} (no exposure–outcome overlap).}
}
\value{
A named parameter list, identical in structure to \code{set_params()} output.
}
\description{
Returns a ready-to-use parameter list corresponding to one of the built-in
simulation scenarios.  The list is identical to what \code{set_params()}
produces, so every element can be overridden afterwards.
}
\examples{
# CHP scenario, small GWAS, no overlap
params <- load_preset("CHP", n = 3e4, snps = 100, exposures = 1, overlap = "none")
data   <- generate_summary(params)

# Start from a preset, then tweak one thing
params <- load_preset("UHP_CHP", n = 1e5, snps = 500, exposures = 3, overlap = "full")
params$true_causal_effects <- c(0.1, 0.2, 0.3)
data <- generate_summary(params)
}
\seealso{
\code{\link{set_params}}, \code{\link{list_presets}}
}
